# 实施状态

更新时间：2026-09-12

## 本轮已实现并验证

- 测试、安装检查和验收演示使用独立临时数据库；默认学习库未被测试初始化。演示种子只接受没有快照的数据库。
- Windows 时区依赖、项目 .env 加载、LEARNING_DATA_DIR 和 npm lark-cli 原生程序解析。
- SQLite v1→v2→v3→v4→v5 迁移；升级前备份，未知新版拒绝打开，业务嵌套事务使用 savepoint。
- 确认边界后刷新真实文本；A→B→A 内容回退正确切换当前版本；并发旧同步不能覆盖后发且已完成的同步。
- 过期候选拒绝批准，疑似移动/重建必须明确决定身份；同步不删除学习事实。
- 图片与嵌入 Sheet 按内容摘要归档，依赖变化产生新依据，缺失素材不分配新任务。CLI 进度文字不会遮住权限错误。
- 答案暴露记录保留；评价修正会重新计算有效通过和撤销达标；模型迟到或重试不覆盖已有人工评价。
- 每日任务可跨日继续，自由练习重试复用结果，代码数量不足会报告。复习先创建的零基础目标计划不会阻止之后生成每日基础任务。
- 导出在新临时目录生成和验证，检查哈希、路径、数据库完整性、外键和事实数量，关闭 Windows 文件句柄。
- 理论作答保存、评价重试、人工更正、复习入口、历史查看、面试对话和个人/模型反思已有页面入口。页面和 README 使用简洁样式。
- 答案评价、面试追问、面试总结、每日反思四个模型模块，各自可编辑服务、模型 ID、提示词和输出上限。
- Agnes 与 OpenRouter 使用 Chat Completions 格式，凭证分离；不自动切换模型或供应商。OpenRouter 可以填写其 :free 模型 ID，实际可用性和额度由服务决定。
- 模型请求保留实际提示词、请求配置、输入、原始返回及状态；用户提示词之外保留固定数据边界与结果格式要求。支持完整 JSON 代码块，拒绝截断和非法引用。
- 独立模型 job 可并行，取消全局串行和固定六秒等待；同一 job 的租约仍防止重复执行。答案先保存，网络失败可重试。已经发起的请求重试时保持原提示词，新配置用于新请求。

## 本机验证

- 自动化测试：115 passed；2 条第三方依赖弃用提示，不影响测试结果。
- Ruff：通过。
- 真实飞书 bot 读取：代码 401 块（1 页）、理论 1106 块（3 页）。用户补齐权限后，图片下载和嵌入 Sheet 读取、二次一致性检查、归档抽样通过。
- Agnes：短请求、合成样例的结构化评价、每日反思、浏览器中的面试追问和总结均实际成功。追问遇过一次 ConnectError，重试成功且回答保留。
- 浏览器：保存理论回答、修改提示词、保存面试回答、结束会话及生成追问/总结已操作验证。
- OpenRouter：默认选项改为用户指定的 nex-agi/nex-n2.5-mini:free。四个模块通过真实接口、隔离库合成输入验证；两两并发执行，评价正确采用 aligned。流式示例返回 3，usage 包含 62 个推理 token，cost=0。此前随机路由曾选到分类模型，另两个免费模型曾返回 429；新模型本轮没有出现此问题。
- 两家模型分档 1/2/4/8 并发短请求共 30 次全部成功。OpenRouter 各档约 1.39/2.18/2.35/2.53 秒；Agnes 约 1.56/11.57/2.60/15.56 秒。这是短请求小样本结果，不代表平台最大并发或长期稳定性。
- 密钥仅在本机忽略配置；没有记录到 SQLite 或项目文档。没有编辑真实飞书文档，没有创建正式 data/learning.db，没有提交或推送 Git。

## 剩余边界

1. 未运行整库全部媒体批量同步，也未对全部候选做人工题目边界验收；抽样可读不代表整库可练习。
2. 同步仍由操作触发，模型仍由按钮显式调用；没有常驻 worker、自动退避调度或跨进程外部账户额度协调。
3. 图片原图可展示和归档，没有自动 OCR 或经验证的视觉评分；参考文字不足不能自动认定通过。
4. 原始方案中的弱项/经典题自动选取、自然语言自由请求、题目新版切换、派生题整理和部分高级历史展示尚未完整接入页面。
5. 每次依赖检查可能重新读取许多图片/表格，整库性能和变化中的媒体一致性还需专项测试；CLI 输出限制目前是捕获后检查。
6. 已补齐理论作答“按当前设置重新评价”入口，旧请求仍固定原配置；面试和反思修改设置后需刷新页面再生成新请求。没有统一的全部请求历史管理页面。
7. 实测验证接口可用和数据流程，不等于评价质量验收；需要用户认可的真实样题覆盖正确同义表达、关键错误、缺证据和提示注入。
8. 恢复接口只验证导出，不替换正式库。完整原始方案 S/B/F 验收矩阵尚未逐项完成。

## 本轮补充修复（2026-09-12）

- 理论重新评价建立新 job 并立即冻结当前设置，复用原 attempt；幂等创建、防止重复完成，保留旧模型结果和人工采用结论。新判定会重算原作答的复习通过记录。
- 数据库升级至 v5，升级前备份；补齐旧评价 job 与 attempt 的关联，冷却时间按服务持久化。
- HTTP 429 解析 Retry-After 秒数或 HTTP 日期，无有效值时默认 60 秒；同库各模块及新请求共享冷却。到期后由用户手动重试，没有新增后台自动调用。
- 模型明确返回 finish_reason=length 时拒绝不完整结果。
- 本轮增加 15 项回归测试，全部 98 项通过；Ruff、JavaScript 语法检查和 9 个页面/接口安装检查通过。前端脚本模拟验证了创建响应丢失时复用请求标识、失败后保留新 job 的重试入口。
- 按用户最新要求，当时不使用 Computer Use 测试（本阶段用户已明确允许浏览器验证）；新增按钮通过接口和脚本验证，没有真实浏览器点击验收。真实模型探测使用合成输入，未修改飞书文档或正式学习数据。

## 启动命令

在项目目录运行：

```powershell
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

打开 http://127.0.0.1:8000，进入“模型设置”修改各模块提示词。已有虚拟环境可直接运行，本机 uv 当前未加入全局 PATH。

本机两家密钥已分别保存为 AGNES_API_KEY 和 OPENROUTER_API_KEY；配置检查确认各一项且互不相同。设置页仅显示是否配置；默认保持 Agnes。切换服务时模型 ID 跟随切换，后端拒绝明显的服务/模型混用。不要把密钥写入提示词或提交到 Git。


## 2026-09-12 首页与递归 Wiki 对齐修复

- Wiki 入口递归读取全部后代、分页和完整路径；已发现并读取八股 41 篇文档，刷题 1 篇。新子文档自动登记，目录错误和不支持类型明确列出；快照完整不代表所有笔记都已转题。
- 手动重新对齐按钮及后台进度，三篇文档并行、全局最多四个素材读取；普通每日/自由练习不触发飞书请求。每轮新增、修改、移除与候选分析有持久化清单。
- 增加第五个独立提示词模块 source_parsing，Agnes 分批分析题目边界；锚点与完整 JSON 校验、一次格式重试，保留失败审计。模型不自动替用户合并题目或删除历史。
- 修复 FastAPI SQLite 跨线程连接错误，统一前端对非 JSON 错误响应的处理。
- 按 Wiki 与正文目录分配八股题量，补齐已有计划缺额但不重画任务。
- 首页半年热力图，每格可点；每日详情分别列出会、不会、待评价、无法判断，以及面试与反思。
- 115 项自动化测试通过。真实 Tabbit 浏览器验证了连续 10 天热力图及全部每日详情、父子配额冲突、纯文本 HTTP 500 处理、重复补齐不加题；桌面和 390px 手机布局无横向页面溢出。
- v6 为递归来源成员和对齐记录增加表，迁移前自动备份；旧作答、评价、任务版本继续保留。


### 最终真实验证（2026-09-12 11:23 后）

- 应用身份 wiki:node:retrieve 已开通并验证；目录遍历完整，刷题 1/1、八股 41/41 份文档读取完毕，权限、分页、文档和素材读取失败均为 0。
- 当前正式库：代码 29 道、八股 184 道，素材状态均 complete。另有代码 20、八股 44 条候选，Agnes 已全部分析；建议仍需确认。八股原文中有 20 个当前未支持的内容块，页面逐篇报告；纯笔记与这些内容不能视作已全部转题。
- hot100 原文标题覆盖：26 项已入库、7 项待确认、1 项只有标题待核验，另有 4 个目录标题；企业 hot100 当前正文只有 no1 一项已入库，另有 1 个目录标题。不能据目录名称声称两个完整的 100 题集已齐。
- 本轮完整对齐耗时：代码 104 秒、八股 181 秒，同时运行。普通抽题已移除飞书与模型等待。真实首页成功补齐 3 道八股，今天现有 2 道代码 + 3 道八股。
- 对照修复前备份，原有 1 条作答各字段未变、2 个原任务题目版本未变；未注入测试学习记录，外键检查无错误。热力图当前为 0 个活跃日，因为真实作答还未提交。
- 独立库已验证连续 10 天可点击热力图及每日详情，并验证会、不会、待评价分组；采用评价变更和无法判断另有回归测试。115 项 pytest 通过、Ruff 与静态 JavaScript 语法检查通过、安装检查 9 个路由均 200。


## 2026-09-12 学生练习流程调整（最新）

- 题量可显式增减；只调整未开始的基础任务，取消采用保留记录的状态变更。打开练习页即开始作答，避免另一标签页调整计划时移出正在做的题。
- 首页仅一个八股展开入口；自由练习支持自然语言选题及全池随机，返回可点击题卡。秋招支持全题库选择和自定义主问题，岗位重点独立保存。
- 个人复盘与模型错题复盘放在首页末尾；模型上下文增加题目、模块、备注和错题集合。严格区分已知情况与建议；查看模型复盘记录相关题目的答案暴露。
- 题库页面以模块和题量为主，连接细节与候选折叠。对齐说明进入运行记录及模型复核输入，但不替代实际读取和删除判定。
- 按描述选题新增独立提示词；统一服务可一处切换，高级设置折叠。默认输出预算按模块设定并给出理由。主服务依然 Agnes，备用不自动切换。
- 修复忽略项在无关编辑后反复出现，显式调整模块可解决改名后的旧配额阻塞。后台模型分析最多两份文档同时进行；同一根目录出现连接失败后，后续候选跳过模型复核但继续读取正文，并明确报告未完成。
- 跨天检查每60秒及页面重新可见时运行，未保存表单不会自动刷新丢失。复习库区分待复习/历史，并提示今日已计数或近期看过答案的情况。
- 数据库v7增加面试岗位重点表，升级自动备份。129项测试通过；新增长度预算、题量编辑、日期变化、题卡API、模型范围校验、故障停止和复盘答案暴露验证。
- 真实浏览器已验证题量增减和保护、随机题卡、自然语言MCP/function call选题、自定义面试、个人复盘、当天错题、未保存输入跨天保护、提示词保存、390px手机布局。
- 实际模型联调发现 Agnes 多次超时/连接中断。选题成功，错题复盘重试成功；对齐读取42份文档成功，但部分模型分析与整轮复核失败，不能宣布模型链路稳定性验收全部通过。最后面试总结真实调用成功；额外整轮说明复核仍超时，代理与直连20秒探测均超时，不能认定代理是根因。

### 本轮交付复核

- 最终129项测试、Ruff和全部JavaScript语法检查通过；模型输出常见格式安全渲染，浏览器验证无HTML执行。
- 浏览器补测：清洁页面跨天自动刷新、复盘原位显示、统一模型切换保留提示词、已保存面试总结展示；中途总结不混入下一次追问上下文，已有总结不遮挡尚未回答的追问。
- 对照本轮before-student-ux.db，原有10个任务、1条作答、213道题、277个版本逐字段未变，外键检查无错误。测试与真实数据隔离。
- 本地学习流程通过；模型稳定性和完整候选整理仍有边界。候选完整拆分编辑器尚未实现，部分泛指题干依靠目录上下文理解，不应宣称题源语义整理全部完成。

## 公开仓库准备

个人飞书来源移入被Git忽略的本机配置，演示和测试使用占位标识；文档移除个人电脑路径。已有数据库、作答和题源未改动。公开仓库不含密钥、学习数据、媒体或备份。
