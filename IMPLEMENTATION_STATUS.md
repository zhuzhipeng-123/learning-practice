# 实施状态

更新：2026-09-21。Python 3.12、FastAPI、Jinja2、原生 JavaScript、SQLite v16；本轮没有新增项目依赖。

主要日常流程、两类自由练习、面试、复习、个人／模型复盘、逐题记录与导出已接入并经过隔离操作验证。请求区分确定失败与处理中；恢复旧请求保留较新草稿；评价与代码自评即时显示实际保存状态。新生成问题与参考配对，显式查看答案才记录暴露；旧题可显式补参考。

- 最新 447 项隔离回归全部通过，无跳过；Ruff、Python 编译和 diff 空白检查通过。
- 独立数据目录与 `127.0.0.1:18931` 的真实 Uvicorn 进程已启动并正常关闭；健康页、今天、复习、来源和设置均返回 200。该探测未调用外部服务，不代表真实飞书或 Agnes 联调。
- 今日八股范围只展示 Wiki 根“八股”下的41份已登记子文档，不再展示文档内449个标题节点。默认折叠且全选；29份有题文档双列显示，12份空文档二级折叠，旧细粒度偏好在下次确认时归并到所属文档。
- 今日页以默认展开的学习足迹开场，练习流程居中，并以默认展开的个人／模型复盘收尾；两处仍可手动收起。
- Wheel 已在独立构建环境生成，并安装到另一干净 Python 3.12 环境；9 个入口路由与全部打包资源校验通过。
- 模型功能统一使用 Agnes。11 个模块保留独立模型、提示词和输出预算；密钥仅从本机环境读取，历史请求继续保留冻结配置。
- 本轮先在 `127.0.0.1:18765` 的隔离服务完成桌面与 390px 终审；获授权后启动正式 8000 服务，将正式库从 v12 事务迁移到 v15并生成迁移前备份。数据库完整性为 `ok`，迁移前后题目、版本、任务、作答、复习依据和来源数量一致。实际浏览器只读检查5个主页面均为200且无横向溢出。
- 真实Agnes在隔离库运行开场、追问、问答整理、变种、评价、总结和复盘。代码样例21,845组输入通过；不能推广到全部题目。
- 方向／复盘格式不合格最多修正一次，记录拒绝内容与修正指令；问答质量检查也有一次修正上限。模型事实自检仍漏错，关键结论需另行核对。
- 每日复盘只读取实际作答、提问和采用的评价，不把旧模型总结当作原始证据。生成参考不作为面试总结固定评分标准；未回答追问有显式标记。
- 局部来源校正独立保存在reference_correction，按版本绑定，纳入导出，不修改飞书原文或历史版本。

完整证据与真实失败例子见 [历史验收记录](docs/historical-acceptance.md#flow-acceptance)。旧的 174／187／214 项验收记录作为历史证据保留，不代表当前回归数量。

## 9 月 21 日 R01–R12 最小整改

| 项目 | 当前结论 | 本轮处理 |
| --- | --- | --- |
| R01 来源任务恢复 | 已修复 | 状态查询改为只读；仅启动时释放中断任务；后台执行先条件领取，重复调度不能重启终态任务。 |
| R02 模型租约与所有权 | 核心写入竞争已修复，外部取消仍有限制 | SQLite v16 增加稳定执行 ID、心跳租约和 60 分钟采用截止时间；原始响应、格式修正、质量结果、失败和最终采用都校验当前所有者。结果最多采用一次，但提供方不支持幂等时，恢复仍可能重复发出外部请求；同步 HTTP 调用自身不能由 Python 安全强杀。 |
| R03 真实内部路径测试 | 已修复 | 测试不再全局替换来源调度；新增接口到调度、服务、SQLite、只读状态的贯通回归，并在测试结束回收对应后台 Future。 |
| R04 参考标签 | 已修复展示歧义 | 飞书来源显示“来源笔记参考”，不再暗示逐项人工核验；自动评分和复习资格规则未改变。 |
| R05 运行版本扫描 | 测量后暂缓 | 保护仍覆盖程序文件变化；当前仓库暖态扫描约 10ms/次。没有用 TTL 或跳过 GET 制造一致性盲窗。 |
| R06 复习查询 | 已修复主要 N+1 | 今日通过和最近暴露改为两次固定批量查询；保留原排序与学习规则。历史 HTML 仍未分页，真实规模增长后再测量。 |
| R07 CLI 输出上限 | 已修复 | stdout/stderr 并发有限读取，任一通道超限或超时即终止并回收子进程；继续使用 argv 与 `shell=False`。 |
| R08 备份 | 严格备份已满足，抢救模式暂缓 | 继续只把完整且校验通过的 ZIP 称为备份，不增加可能误导的不完整快照。 |
| R09 模型诊断 | 已修复 | 日志增加任务/执行 ID、阶段、耗时、错误类别与可用 token 用量，不记录提示正文、回答或密钥。 |
| R10 路由整理 | 暂缓 | 当前拆分足够，本轮不为整理而做全仓重构。 |
| R11 来源数量 | 已修复 | 初始化接口返回数据库中的真实来源总数，重复调用保持幂等。 |
| R12 状态文档 | 已修复 | 当前测试结果只在本文件维护；README 链接到本文件，不再重复写易过期的“当前数量”。 |

SQLite v16 仅新增 `model_job_execution` 表，不改学习事实和历史快照。已有数据库启动迁移前仍按现有机制备份；若必须回退旧程序，需要恢复迁移前备份，旧程序不会识别 v16 的执行所有权语义。本轮没有迁移正式数据库、启动正式服务、调用真实模型或读取真实飞书。

## 当前边界

模型幻觉、同模型自检漏判、叙述概括偏差仍存在。B4 使用56例合成材料完成 A 基线，并累计进行了11次真实 Agnes 调用尝试；B 方案始终未通过 smoke，出现截断、越界/重复块、改变顺序和证据引文不属于对应块等问题，包含一次格式修正后仍失败。按预设门槛选择保留 A，不把模型扩展接入自动对齐发布；Agnes 原有评价、生成、面试和复盘用途不变。全量题库逐题事实审计、OCR／图片自动评价、复杂候选完整多题拆分编辑器、负载与漏洞扫描未完成。只面向本机单人单进程；模型任务的数据库采用有绝对截止时间，但底层同步 HTTP 调用仍依赖连接/读取超时退出，恢复时可能产生外部重复调用。浏览器草稿不跨设备共享，禁用存储时不保证刷新恢复。本轮未调用真实飞书。2 条第三方弃用提示保留。
