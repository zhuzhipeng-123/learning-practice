# 自由练习与页面交互修复验收

日期：2026-09-12。此次追加修复基于已有未提交改动；未执行 Git 暂存、提交或推送。

## 已修复的问题

最初通过实际浏览器复现了三个问题：代码与八股共用一个描述；生成代码题时八股按钮也被禁用；离开页面再返回时当前题单消失。

- 代码与八股现在分别拥有题源、抽题方式、内容描述、题量、状态和恢复按钮。仅同类正在生成的批次需要等待，另一类可同时操作。
- 生成请求和题单保存在 SQLite；后台生成不依赖页面是否打开。离开、刷新、重新打开时恢复进度和结果；编辑下一批设置不会隐藏现有题单。旧版未完成题目保留在历史题单。
- 响应丢失按原请求恢复，重复提交不重复创建；失败保留已有题目。服务重启后中断批次可显式重试，保留原请求、模型配置快照和日期归属。仅打开页面不会重新生成。

## 扩展审查与修复

围绕共享控件、共享状态、整页刷新、迟到响应和固定默认值，阅读并检查了首页、练习、面试、复盘、模型设置和题源交互。

- 首页保存个人复盘、保存题量只更新对应区域；不打断正在生成的模型复盘，不清空其他草稿。修改题量时已保存题单仍显示。
- 面试保存回答和生成追问就地更新；总结与对话使用独立状态。切换整理题目时旧响应不能覆盖新草稿，取消后迟到响应不能重开编辑器。
- 模型模块可独立保存；有模块保存中时统一切换按钮等待相关操作完成。统一切换不会刷新页面或丢失未保存提示词；默认模型名读取后端配置。
- 题源同步仅锁定同一来源树，其他根来源保持可操作；异常报告和添加来源失败有恢复路径。
- 作答控件按同一学习事实限制并发；共享锁保留原禁用状态，避免先完成的请求提前解锁另一请求。

## 实际验证

| 检查 | 结果 |
|---|---|
| 完整自动化测试 | 187 passed，包含原有十天学习／导出恢复测试及新增 13 项批次测试。2 条第三方弃用提示仍在。 |
| 新增后端覆盖 | 代码慢而八股先完成、跨连接幂等、跨日同请求／失败重试、失败不丢旧结果、零结果保留上一批、启动中断恢复、v9 旧库迁移和旧自由题单保留、非法参数。 |
| 静态检查 | Ruff、git diff --check 通过；13 个页面 JavaScript 文件语法检查通过。没有新增依赖。 |
| 自由练习浏览器 | 独立描述、同时生成、生成中离开再返回、刷新设置、编辑不隐藏题单、服务端接受后丢响应再恢复、失败后按原批次重试、跨标签去重、422 修正后重交、禁用浏览器存储仍恢复服务端结果。 |
| 其他浏览器流程 | 首页三个区域同时操作不整页刷新；模型模块并发保存及统一切换保留草稿；不同题源树锁定隔离；面试追问与总结并行、迟到整理结果与取消防护。 |
| 正式页面 | 首页、自由练习、面试入口、历史、复习、题源、设置，均在 390px 和 1280px 宽度 HTTP 200，无脚本错误和页面级横向溢出；自由练习显示 6 道旧版未完成题目。 |

本次模型与异常场景使用独立测试数据库和本地模型替身，没有修改正式飞书文档或向模型发送正式作答。上一轮的真实 Agnes 8/8 小样本与飞书单篇抽查属于上一轮证据，此次未重新调用外部服务。

截图工具两次等待超时，且当前浏览器不支持请求前台模式；因此此次完成的是 DOM 尺寸、可见性、按钮行为和业务状态验证，未完成截图目视验收。两条浏览器脚本曾因测试前提错误中止：初版失败文案断言与实际错误净化不一致、方向开场不计入对话轮次；检查已执行状态、修正预期并继续验证后通过。窄屏脚本仅在最后截图步骤超时，之前的交互断言已完成；随后又单独核验正式页面尺寸与恢复题量。

## 本机更新与数据保护

正式服务已在 http://127.0.0.1:8000 重启，健康检查通过。数据库 v9 → v10 升级前自动生成 `data/pre-migration-v9-20260912134625324409.db`（13,074,432 字节）。重启前没有运行中的模型任务。

升级前后均为：536 道题、606 个题目版本、4 条作答、0 条反思、42 个来源；外键错误 0、完整性检查 ok。此次只读正式页面，没有在正式库生成测试题或修改设置。

## 当前边界

应用仍面向本机单人、单进程使用；后台恢复要求应用服务继续运行。服务被终止时不自动重新花费模型调用，返回后由用户重试中断批次。尚未验证生产多进程或跨设备并发，也未逐题审核整个真实题库的内容质量。浏览器设置草稿不跨设备；禁用存储时数据库中已接受的批次仍可恢复，未提交草稿不能保证刷新保留。

项目规则、README.html、README.md、实施状态和设计补充已同步更新。可选英文提交说明：`Persist independent free practice batches and isolate page actions`。


## 持续使用追加审查

发现并修复了两项问题：页面请求缺少总等待期限；恢复旧作答后会清掉网络报错期间新写的草稿。现在读取请求默认 15 秒、写入或同步模型请求默认 120 秒，期限覆盖响应正文读取；超时作为未知结果处理，原请求标识仍保留，按钮走已有恢复路径。恢复旧作答后，新改动单独显示为未提交草稿，用户可复制另存并主动清除。

- 实际浏览器注入“服务器已接受请求、响应正文一直不结束”，缩短测试计时后验证超时恢复：相同批次、相同请求标识、不重复生成、另一面板仍可操作、上一批题单保留。
- 实际浏览器验证旧备注已保存但响应丢失 → 补写新备注 → 恢复旧提交 → 新草稿在完成页及刷新后仍可复制，正式作答仍是原提交，只有显式清除才移除草稿。
- 新增后端原题／变式两个崩溃点测试：学习任务已提交但批次回执还未写入时中断，重启后恢复原任务，不重复创建，也不重复调用已经完成的模型任务。
- 新增备份恢复测试：已完成批次题单与未完成请求一并导出，在新目录恢复并完成中断批次，外键检查通过。
- 完整测试 187 passed，2 条第三方弃用提示；Ruff 通过。期限辅助函数替换后，首页／设置并发与来源异常浏览器回归通过；13 个脚本语法检查通过。
- 通过 importlib.metadata 与 uv.lock 逐项核对，Python 3.12.10、33 个已安装包版本一致，无额外包。唯一未安装的 uvloop 在锁文件中明确排除 Windows。当前终端找不到 uv，未完成 uv sync --locked --dry-run，也未重新安装依赖。

### 完成核对

| 用户要求 | 当前可核对证据 |
|---|---|
| 分开代码、八股描述和设置 | 独立模板面板、按类型持久化批次；浏览器分别填写与刷新恢复通过。 |
| 一边生成不阻止另一边 | 两个工作线程、按类型活动批次约束；延迟代码生成时八股先完成的后端和浏览器检查通过。 |
| 离开回来题目仍在 | 数据库存储和页面恢复；导航、刷新、重启、导出恢复及旧题单可见性通过。 |
| 从代码、布局和异常角度审查 | 共享状态与锁、整页刷新、迟到响应、失败重试、请求挂起、草稿丢失均已检查；七个页面两种宽度测量通过，截图限制已单列。 |
| 大部分操作互不影响、不写死业务状态 | 首页区域与设置模块独立，面试同一会话的写操作串行；题源按实际来源树隔离；状态来自数据库，模型默认名来自配置，数量上限属于明确的请求边界。 |
| 十天实际业务模拟、足迹／反思／题库变化 | 当前完整回归包含 test_ten_day_acceptance.py；前轮验收记录列出 29 次活动、20 份反思、源更新／失败和恢复页面证据。 |
| 原评审 R01–R15 | 前轮验收逐项记录实现和行为测试；当前 187 项完整回归包含对应套件，未恢复旧题默认或弱化五次严格超过七天的规则。 |
| 现有数据与本地运行 | v10 升级备份和前后数据一致检查通过，正式七页只读检查通过；此次追加只修改脚本／模板和测试，不再次迁移数据。 |

以上支持当前单人本机使用路径及已验证异常可恢复；不把有限测试表述为永久零故障承诺。真实模型／网络暂不可用时保留学习事实并提供恢复动作，自动生成内容仍需要人的判断。
