# 全流程模拟与修复验收

日期：2026-09-13。结论：已覆盖的主要操作与异常恢复通过；模型生成内容仍存在事实误判风险，不能承诺任意情景零 bug 或答案全部正确。

## 本次修复的卡点

| 触发情景 | 原来的卡点 | 现在的处理与验证 |
|---|---|---|
| 面试保存成功但响应丢失，继续写新草稿后恢复 | 恢复旧请求可能清空新内容；刷新也会用旧载荷覆盖草稿 | 按真正恢复的载荷判断是否清空，优先保留较新的浏览器草稿；真实浏览器注入丢响应并刷新验证 |
| 方向生成明确失败，改方向重试 | 被旧的未确认请求挡住 | 后端明确返回已失败／仍在处理；失败可重新填写，处理中和限流保留原键与载荷；API及浏览器验证 |
| 从经典题／错题池抽题后响应未知 | 修改岗位后没有恢复原请求的入口 | 增加恢复按钮，沿用原岗位与请求标识；浏览器验证两次请求相同 |
| 代码面试自评结果丢失或刷新 | 缺少恢复入口，成功后按钮仍可重复提交，刷新看不到自评 | 原请求恢复；显示实际保存结果，成功及刷新后禁用重复自评；API、浏览器与幂等验证 |
| 理论评价、重评、人工采用后 | 上方仍显示等待评价，重试按钮与实际状态不一致 | 单独刷新评价摘要、复习计数和重试状态；保留编辑及参考区；浏览器验证人工采用不被模型覆盖 |
| 随机推荐方向写成长段落或空对象 | 格式被拒绝，用户反复遇到失败 | 明确短标题协议，格式不合格最多再生成一次；保留拒绝内容和修正指令，仍不合格则可恢复地报错；真实 Agnes 最终返回三个方向 |
| 每日复盘引用旧模型总结、内部编号和转义文本 | 错误可能层层传播，正文不易读 | 仅使用实际作答、提问和采用的评价；补充自评状态及未答追问标识；编号／字段名／重复转义不直接保存，最多修正一次 |
| 面试总结把未答追问视为已经答错 | 未核实生成参考被当作固定评分标准 | 总结不再读取生成参考作为评分依据，提供未作答追问标识；真实新总结已将该项写为“不确定” |
| 模型总结输出 Markdown 表格 | 页面显示原始竖线，比较难读 | 增加使用安全 DOM 节点的表格显示，不执行模型 HTML；窄屏表格在自身区域滚动 |

## 按使用顺序验证

| 环节 | 本次浏览器实际操作 | 自动化／真实模型补充 |
|---|---|---|
| 今日安排 | 非法题量、保存代码／八股各1题、打开题单 | 模块配额、已开始任务保护、旧计划／跨天、缺题与补题回归 |
| 理论作答 | 空输入、草稿刷新、保存、看参考、看历史答案 | 同请求幂等、版本冻结、参考曝光、失效输入回归；真实 Agnes 正确评价一题 |
| 评价 | 开始评价、人工改为需复习、再由模型重评 | 人工采用保留，状态即时一致；并发租约与错误状态回归 |
| 代码练习 | 备注、不会做、查看已保存自评 | 会做／不会做与复习计数回归 |
| 自由练习 | 代码原题与八股按描述变种同时生成、刷新保留题单 | 批次失败、超时、丢响应、跨标签与来源选择回归；真实 Agnes 生成代码及理论变种各1题 |
| 面试 | 开场方向失败恢复、空会话追问、保存、丢响应恢复、刷新、追问、看每题参考 | 真实 Agnes 开场／追问／对应答案、旧题显式补答案、迟到回答隔离回归 |
| 整理复习题 | 查看对话、整理追问、取消、再次整理、确认加入 | 真实 Agnes 生成整理草稿；未确认不入库、来源关系及重复确认回归 |
| 面试结束与自评 | 未保存草稿阻止结束、结束、生成总结、代码自评恢复及刷新 | 真实 Agnes 总结；同会话操作锁、主任务计数与不可重复自评回归 |
| 复习 | 加入复习、从复习库开始练习 | 十天隔离模拟、同日去重、曝光24小时、5次且首末严格超过7天、版本变更回归 |
| 复盘 | 空输入、个人保存、模型生成中个人仍可保存、刷新与回看 | 真实 Agnes 复盘与格式重试；作者隔离、迟到响应及原始证据回归 |
| 历史 | 查看历史入口、逐题记录和已保存内容 | 分页第101条可到达、版本历史、保护答案不提前显示回归 |
| 题源 | 页面与移动端检查 | 用分页飞书替身跑实际读取、同步、候选确认／拒绝、部分失败和完整遍历保护；本次未再操作真实飞书 |
| 模型设置 | Agnes 单模块保存、刷新确认、还原测试修改 | 独立设置、快照冻结、并发设置保护回归 |
| 导出 | 浏览器请求导出 ZIP 并调用恢复校验 | 隔离真实模型库也导出并校验；数据库完整性、外键、清单与文件校验 |

## 验证证据与范围

- 当次自动回归：**214 passed，3 deselected**。自动化回归未调用外部服务；另行真实 Agnes 检查使用隔离数据库，未修改正式学习库或真实飞书文档。
- Ruff 通过。diff 空白检查通过。保留2条第三方弃用提示；本次没有新增依赖。
- 4组本轮浏览器操作程序通过；实际注入了“已保存但响应丢失”、明确502失败和仍处理中409。
- 7个主要页面分别检查390px、1280px宽度，均无页面级横向溢出、无脚本错误。
- 新生成的字符首个唯一位置 Python 解法，经人工阅读后执行：字符集 abcd、长度0至7的 **21,845** 个输入与独立朴素答案一致。它只证明这道代码样例的覆盖结果，不代表全部生成代码正确。
- 真实模型检查使用隔离数据库，Agnes。正式学习库没有加入模拟作答、面试或复盘；不修改真实飞书文档。
- 代码保留在现有工作区，没有执行 git add、commit、push。

## 模型内容仍然存在的风险

**不能把模型自检通过当成已经核实。** 本次真实生成开场参考把 `asyncio.wait_for` 描述为能包装任意阻塞调用；整理题参考又声称唯一索引避免锁竞争。这些答案曾通过同模型检查，人工核对仍发现不准确。

Python 文档区分可等待对象和阻塞函数；直接执行阻塞函数会阻塞事件循环，`to_thread`等隔离方式也不等于远端副作用已撤销。[Python asyncio 文档](https://docs.python.org/3.12/library/asyncio-task.html)

唯一索引能约束并发写入，但仍可能等待另一事务结束；不能说没有锁竞争。超时、冲突、隔离级别和外部副作用需按实现处理。[PostgreSQL 唯一性检查](https://www.postgresql.org/docs/17/index-unique-checks.html)

Redis 锁有租约与过期约束；单个 SETNX 加 TTL 并不自动保证外部业务副作用只执行一次。[Redis 分布式锁文档](https://redis.io/docs/latest/develop/clients/patterns/distributed-locks/)

本轮已削弱错误参考和旧总结传播，已将未答追问单独标出。复盘仍可能出现轻微概括偏差，例如把主任务说成模块、把提问误称为回应；目前没有独立事实验证器证明全部叙述准确。建议把模型参考与总结当作练习辅助，关键技术结论核对官方资料；人工采用、显式参考查看和原始记录始终保留。

上一阶段还发现真实来源中的点积／余弦混淆及 MLA 描述问题。局部核对结果单独按题目版本存储，包含出处，不改写原始飞书快照；不能据此宣布整个题库准确。问答完整性、范围和质量检查至多修正一次后再决定发布，不完整问答不会进入新题单。查看答案才记录暴露。

## 未覆盖或未完成

没有穷举所有浏览器、设备、网络断开时刻、全量真实题库和所有模型输出；没有做负载压力、漏洞扫描、OCR／图片自动评价或完整复杂候选拆分编辑器。本轮只针对单机单用户使用流程验证。


## 正式服务交付核验

服务已在 http://127.0.0.1:8000 更新并重新启动。重启前已做SQLite备份并确认无运行中的生成任务；重启后七个主要页面均HTTP200，新的失败／处理中响应协议已确认生效。

前后39张表逐表全行SHA-256一致：537道题、607个版本、35项任务、5次作答、4场面试、0条正式面试对话、0份正式复盘和2项既有版本校正保持不变。数据库完整性ok，外键错误0。没有模拟学习数据进入正式库。

重新加载正在打开的页面即可使用新版本。
